<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link rel="stylesheet" href="/node_modules/bulma/bulma.sass">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="columns is-flex-direction-column is-justify-content-space-around">
    <header class="columns is-flex is-justify-content-space-between is-align-items-center p-2">
      <div class="column">
        <h1 class="title">
          <span role="img" aria-label="money">💸</span>
          <a href="/">Tech Blog</a>
        </h1>
      </div>
      <nav class="column">
        <!-- todo: Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/dashboard">Dashboard</a> | <button class="button is-text" id="logout">Logout</button>
        {{else}}
        <a href="/dashboard">Dashboard</a>
        {{/if}}
      </nav>
    </header>
    <main class="container mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- todo: Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
